# ============================================================================
# Environment Variables — Enterprise Serverless Platform
# ============================================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# ⚠️  DO NOT commit the .env file to version control!

# ── GCP Project ────────────────────────────────────────────
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# ── PostgreSQL (Local Docker Compose) ──────────────────────
POSTGRES_DB=platform_db
POSTGRES_USER=platform_user
POSTGRES_PASSWORD=local_dev_password_change_me

# ── Database Connection (used by app service) ─────────────
DB_HOST=db
DB_PORT=5432
DB_NAME=platform_db
DB_USER=platform_user
DB_PASSWORD=local_dev_password_change_me

# ── Cloud Storage ─────────────────────────────────────────
GCS_BUCKET_NAME=your-upload-bucket-name

# ── Application ──────────────────────────────────────────
ENVIRONMENT=local
PORT=8080
